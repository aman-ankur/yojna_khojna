# Environment variables for the Yojna Khojna backend pipeline

# Weaviate Configuration
WEAVIATE_URL="http://localhost:8080"
WEAVIATE_CLASS_NAME="YojnaChunk" # Default class name for indexed chunks
# WEAVIATE_GRPC_URL="http://localhost:50051" # Optional, if needed for specific client connections

# Embedding Model Configuration
EMBEDDING_MODEL_NAME="paraphrase-multilingual-mpnet-base-v2"

# Default path for test PDF processing when running main_pipeline.py directly
# Use a path relative to the backend directory or an absolute path.
# Make sure this file exists for the main_pipeline test run.
DEFAULT_TEST_PDF="./test_docs/small_awaas_yojna.pdf"

# Add any future API Keys here (e.g., OPENAI_API_KEY="")
# Remember to add them to .gitignore if they are sensitive!

# Add this line for the Anthropic API key
ANTHROPIC_API_KEY=""

# Weaviate Text Key
WEAVIATE_TEXT_KEY="text" 